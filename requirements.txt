# Core
typer>=0.12
rich>=13
pyyaml>=6
httpx>=0.27

# API server
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2

# AI providers — install the one(s) you use:
# pip install debugai[openai]
openai>=1.30

# Optional: Anthropic
# anthropic>=0.29

# Optional: local models via Ollama (no extra install needed, uses httpx)

# Optional: RAG / knowledge base
# chromadb>=0.5
